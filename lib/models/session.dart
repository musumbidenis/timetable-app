class Session{
  
  final String unitCode;
  final String unitTitle;
  final String sessionStart;
  final String sessionStop;

  Session(this.unitCode, this.unitTitle, this.sessionStart, this.sessionStop);

}
